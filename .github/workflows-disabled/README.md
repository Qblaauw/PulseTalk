# Archived GitHub Actions deployment workflows

Release workflows here are retained for rollback and historical reference. Their
`.yml.disabled` suffix prevents GitHub from loading them as workflows.

PulseTalq currently validates, packages, stages, and publishes releases from
approved local hosts through the root `Taskfile.yml`. See
`docs/DEPLOYMENT.md` before restoring any workflow.
